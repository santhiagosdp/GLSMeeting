instalar o Python
python -m pip install -U pip
pip install django
pip install --upgrade python-gitlab
pip install xhtml2pdf
pip install psycopg2  #postgree
instalar o PostgreSQL no pc
Criar banco de Dados do PostgreSQL (conforme Settings)




